// Vamos una configuración a las características como viene por defecto Vite
import { defineConfig } from 'vite';

export default defineConfig({
    base: '/blackjack-vite/' // Para que encuentre los CSS y JS el GitHub Pages en docs
});